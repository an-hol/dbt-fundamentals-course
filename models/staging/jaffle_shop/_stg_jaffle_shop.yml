models:
  - name: stg_jaffle_shop_customers
    description: The grain is one unique customer per row
    columns:
      - name: customer_id
        description: Primary key for customers table
        data_tests:
          - unique
          - not_null

  - name: stg_jaffle_shop_orders
    description: One order per order
    columns:
      - name: order_id
        description: Primary key for orders table
        data_tests:
          - unique
          - not_null

      - name: order_status
        description: "{{ doc('order_status') }}"
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - completed
                  - shipped
                  - placed
                  - returned
                  - return_pending
      - name: customer_id
        data_tests:
          - relationships:
              arguments:
                field: customer_id
                to: ref('stg_jaffle_shop_customers')
              